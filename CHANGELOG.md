# CHANGELOG

（此前的历史未记录，当前状态见 CLAUDE.md）

## 2026-09-11

- 重构：抽取 `readoutCard()` 统一四处重复的统计卡片模板；抽取 `twoBandSplit()` 合并 `bestMixedLayout` 里 X/Z 轴的镜像代码（用 20 万组随机输入验证行为完全一致）。所有输入 `<label>` 补上 `for` 属性；外箱/卡板尺寸放不下时改为给出明确提示，不再显示一堆 0。影响范围：STEP01/02/03 结果渲染 + 全部输入 label。
- 新增：卡板/货柜规格支持存为常用预设（`localStorage`，只对这两者开放，彩盒/外箱尺寸每个产品不同不适合存）；汇总区加"复制文字摘要"（写剪贴板，`file://`离线场景自动降级）和"打印/导出 PDF"按钮及对应 `@media print` 样式；STEP03 新增"货柜内允许卡板堆叠"勾选框，按内高自动算层数并给出承重风险提示，同时补上此前缺失的"卡板堆高超过货柜内高"校验。
- 修复：等轴视图渲染问题（用户截图反馈"看起来混乱"）——虚线外框改画在箱体下方（此前画在上面会跟箱体边线重叠打架）、箱子间距从 6% 加大到 10%、尺寸标注文字加白色描边光晕防止被线条挡住、虚线外框整体调淡不再抢视觉重心。
- 新增：外箱侧放/倒放支持（`bestOrientedLayout`），STEP02/STEP03 地板散装新增"允许外箱侧放/倒放"勾选框，默认不勾选（行为不变）。采用启发式近似算法（整批箱子统一尝试 3 种朝向取最优，不做逐箱混合朝向的数学最优解），复用已有的 `bestMixedLayout` 做每种朝向下的底面横竖混排。

## 2026-08-05

- 界面改为黑白工业工程图风格：DM Sans / DM Mono / Noto Sans SC 字体，唯一强调色 `#b23a2e`，标题区改为工程图纸"title block"排版。影响范围：全部三个 STEP 的样式。
- 修复 STEP01 推荐外箱算法的真实缺陷：原先只按数学"体积利用率"排序，会推荐出 1×1×23 这类细长条箱（利用率高但不实用）。加入长宽高比例过滤（优先 ≤3，找不到再放宽），并允许 a×b×c ≥ N（不再要求 N 必须整除），多余格位显示为"空位"。
- 三个 STEP 的示意图全部改为等轴（2.5D）视图，可直观看到每个彩盒/外箱/卡板在容器里的排列方式和留空格位；格子数过多时自动降级为网格分隔线展示以保证性能。
- 卡板/货柜底面排列新增"横竖混排"支持（`bestMixedLayout`）：原先整层箱子必须同一个方向摆放，现在允许把剩余空间切成两块、其中一块旋转90°摆放来填补边角，取数量最多的方案（等轴图里补边区用略深的灰色区分）。暂不支持箱子侧放/倒放（垂直方向），高度朝向仍固定直立。
